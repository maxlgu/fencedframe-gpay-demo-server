doctype html
html(lang="en")
  head
    title Static Files
    meta(charset='utf-8')
    link(rel="stylesheet", href="/static/main.css")
  body
    p This is a static file serving example.

script.
    // If there's cookie, grab the PII info from the cookie
    // Otherwise, display nothing
    if (document.cookie) 